---
title: Moduli del corso
---

# Moduli del corso

Il corso *CLI - Command Line Interface* è strutturato in **moduli progressivi**.

Ogni modulo:

- introduce nuovi concetti
- risolve problemi che emergono dal progetto
- estende ciò che è stato costruito nei moduli precedenti

Non esistono moduli indipendenti: **il corso è cumulativo**.

---

## Come usare i moduli

- segui i moduli in ordine
- non saltare le lezioni iniziali
- usa il progetto come riferimento costante
- confronta teoria, esempi ed esercizi con il modello dati

---

## Elenco dei moduli

### Modulo 01 — Fondamenti della CLI
Costruisce il **modello mentale corretto**.

Contenuti chiave:

- introduzione storico‑generale (breve)
- CLI ≠ terminale
- shell e interprete dei comandi
- filosofia UNIX

👉 [Vai al modulo](./01-fondamenti.md)

---

## Modulo 02 — Filesystem
Introduce la **struttura dei file** e la navigazione.

Contenuti chiave:

- gerarchia standard
- path assoluto e relativo
- working directory
- concetti chiave (file, directory, inode, link)

👉 [Vai al modulo](./02-filesystem.md)


---

<!-- 


---

## Modulo 03 — Permessi e utenti
Spiega **sicurezza e proprietà** dei file.

Contenuti chiave:

- modello UNIX (user / group / others)
- permessi (r, w, x)
- ownership
- cenni su privilegi e sicurezza

---

## Modulo 04 — Comandi core
Primi comandi **operativi essenziali**.

Contenuti chiave:

- manipolazione file: `cp`, `mv`, `rm`, `mkdir`, `rmdir`, `touch`
- visualizzazione: `cat`, `less`, `more`, `head`, `tail`
- ricerca: `find`, `grep`, `locate`

---

## Modulo 05 — Pipe e redirection
Costruisce il **flusso dei dati** tra comandi.

Contenuti chiave:

- stream standard (stdin, stdout, stderr)
- redirection (`>`, `>>`, `<`, `2>`)
- pipe (`|`)
- concetto di pipeline

---

## Modulo 06 — Variabili e ambiente
Spiega il **contesto di esecuzione**.

Contenuti chiave:

- variabili di shell
- variabili d’ambiente
- `PATH`
- contesto di esecuzione dei comandi

---

## Modulo 07 — Processi e job control
Introduce **processi e gestione dell’esecuzione**.

Contenuti chiave:
- concetto di processo
- visualizzazione processi
- job in foreground / background
- segnali e terminazione

---

## Modulo 08 — Debug, log e monitoraggio
Focus su **diagnostica di base**.

Contenuti chiave:

- log di sistema
- analisi in tempo reale
- spazio disco e risorse
- diagnostica di base

---

## Modulo 09 — Networking da CLI
Chiude con **rete e accesso remoto**.

Contenuti chiave:

- configurazione rete
- diagnostica di connettività
- HTTP e trasferimento file
- accesso remoto

---

 -->

## Output complessivo del corso

Al termine di tutti i moduli, lo studente avrà:

- una comprensione solida del modello UNIX e del ruolo della shell
- capacità di navigare e manipolare il filesystem in modo sicuro ed efficiente
- padronanza dei permessi, della proprietà dei file e delle buone pratiche di sicurezza
- uso quotidiano dei comandi core per ispezionare, cercare e trasformare dati
- abilità nel comporre pipeline con pipe e redirection per automatizzare task ripetitivi
- confidenza con variabili, ambiente e contesto di esecuzione (`PATH` incluso)
- strumenti di base per gestire processi, job control e segnali
- metodo per fare debug, leggere log e monitorare risorse da terminale
- competenze essenziali di networking da CLI per diagnosi e accesso remoto

---

## Navigazione

- [Home corso](../index.md)
- [Community](../../../community/index.md)

